name: Deploy Venti Bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /root/Venti-bot
            cp .env /root/.env.venti.deploy-backup
            git stash --include-untracked
            git pull origin main
            git stash pop || true
            cp /root/.env.venti.deploy-backup .env
            npm install
            pm2 restart venti-bot
